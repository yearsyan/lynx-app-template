# DOM.querySelectorAll

- `DOM.querySelectorAll` - CSS selector query all
- Input: `{nodeId: NodeId, selector: string}`
- Output: `{nodeIds: Array<NodeId>}`
- Description: Finds all elements matching the CSS selector

### Attribute Operations
