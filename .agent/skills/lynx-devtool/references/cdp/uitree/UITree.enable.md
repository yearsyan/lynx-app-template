# UITree.enable

- `UITree.enable` - Enable the UITree domain.
- Input:
  - `useCompression` (boolean): Whether to compress the tree data. Set to `false` for human-readable output.
- Output: `{}`
- Description: Enables the UITree domain for the current session. Must be called before `UITree.getLynxUITree`.
