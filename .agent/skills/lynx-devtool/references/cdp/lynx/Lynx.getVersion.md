# Lynx.getVersion

Scope: This is a LynxView-specific CDP extension. For WebView targets, use standard Chrome DevTools Protocol version or target metadata methods instead; this method may return `method not found`.

- `Lynx.getVersion` - Get version
- Input: None
- Output: `string`
- Description: Returns the current Lynx framework version

### Data Transfer
