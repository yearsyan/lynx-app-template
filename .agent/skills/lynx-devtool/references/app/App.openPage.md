# App.openPage

- `App.openPage` - Open a page in the host app.
- Input: `{ "url": string }`
- Output: `{ "code": number, "message": string }`
- Description: Open any page or popup within the host app using a suitable container. Similar to the host's scan code capability.
